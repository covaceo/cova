import assert from "node:assert/strict";
import { createHash } from "node:crypto";
import { existsSync, readFileSync } from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";

const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");
const read = (...parts) => readFileSync(path.join(root, ...parts), "utf8");
const hash = (...parts) => createHash("sha256").update(readFileSync(path.join(root, ...parts))).digest("hex");

const app = read("src", "App.tsx");
const propFirms = read("src", "lib", "propFirms.ts");
const importDesk = read("src", "components", "ImportDesk.tsx");
const importPanels = read("src", "components", "ImportPanels.tsx");
const dashboard = read("src", "components", "DashboardView.tsx");
const attribution = read("src", "components", "RithmicAttribution.tsx");
const endpoint = read("api", "rithmic", "sync.js");
const statusEndpoint = read("api", "rithmic", "status.js");
const limiter = read("api", "_lib", "rithmic-limit.js");
const bridge = read("api", "_lib", "rithmic-service.js");
const vercel = read("vercel.json");
const rithmicSyncStart = importDesk.indexOf("async function syncRithmic");
const rithmicFinallyStart = importDesk.indexOf("} finally {", rithmicSyncStart);
const rithmicSyncEnd = importDesk.indexOf("async function startTradovateConnect", rithmicFinallyStart);
assert.ok(rithmicFinallyStart > rithmicSyncStart && rithmicSyncEnd > rithmicFinallyStart, "The Rithmic finalization path must remain inspectable.");
const rithmicFinallyBlock = importDesk.slice(rithmicFinallyStart, rithmicSyncEnd);
const rithmicZeroTradeStart = importDesk.indexOf("if (!data.csv || tradeCount <= 0)", rithmicSyncStart);
const rithmicVerifiedLedgerStart = importDesk.indexOf("const verified = parseCsvDetailed(data.csv)", rithmicZeroTradeStart);
const importPrincipalStart = app.indexOf("function getCurrentImportPrincipal");
const prepareImportStart = app.indexOf("function prepareImportCsv", importPrincipalStart);
assert.ok(importPrincipalStart >= 0 && prepareImportStart > importPrincipalStart, "The live import principal boundary must remain source-identifiable.");
const importPrincipalBlock = app.slice(importPrincipalStart, prepareImportStart);
assert.ok(rithmicZeroTradeStart >= 0 && rithmicVerifiedLedgerStart > rithmicZeroTradeStart, "The Rithmic zero-trade branch must remain inspectable.");
const rithmicZeroTradeBranch = importDesk.slice(rithmicZeroTradeStart, rithmicVerifiedLedgerStart);

assert.doesNotMatch(app, /Powered by Rithmic/i, "The approved homepage must not gain a powered-by section.");
assert.match(propFirms, /Rithmic account sync/, "The provider selector must expose the implemented Rithmic connection path.");
assert.doesNotMatch(propFirms, /Rithmic Test connector/, "Customer-facing provider selection must not stay Test-only.");
assert.doesNotMatch(propFirms, /Rithmic direct sync is planned/, "Rithmic must no longer be left as a planned-only CSV route.");
assert.match(importPanels, /systemName: "Rithmic Paper Trading"/, "Paper trading must be the safe default environment.");
assert.match(importPanels, /data-rithmic-environment/, "The user must explicitly choose the Rithmic environment.");
assert.match(importPanels, /Rithmic Paper Trading[\s\S]*Rithmic 01[\s\S]*Rithmic Test/, "Paper, live R01, and provider Test environments must remain explicit.");
assert.doesNotMatch(importPanels, /Test username|Test password|Private Test connector/, "The production connector form must not masquerade as a Test-only preview.");
assert.match(importPanels, /autoComplete="username"/, "The production Rithmic form must label the username field for password managers.");
assert.match(importPanels, /autoComplete="current-password"/, "The production Rithmic password field must be explicit and masked.");
assert.match(importPanels, /data-provider-picker/, "The provider choice needs one compact, testable selector.");
assert.match(importPanels, /active \? "Selected" : "Select"/, "Every provider card needs a clear select/selected action state.");
assert.match(importPanels, /onClick=\{\(\) => selectFirm\(firm\)\}/, "Choosing a provider must select it without immediately starting a connection flow.");
assert.match(importPanels, /data-broker-lifecycle/, "Production status, sync, and disconnect controls must remain available.");
assert.match(importPanels, /checkTradovateStatus[\s\S]*syncTradovate[\s\S]*disconnectBroker/, "The compact selector must preserve existing provider lifecycle controls.");
assert.doesNotMatch(importPanels, /\{firm\.summary\}/, "Provider cards must not repeat explanatory paragraphs.");
assert.doesNotMatch(importPanels, /<ImportStat label="Firm"/, "The selector must not repeat provider metadata in a separate stat strip.");
assert.doesNotMatch(importPanels, /source-route-ledger/, "The selector must not repeat internal routing reasoning below the form.");
assert.match(importPanels, /function changeRithmicEnvironment\(systemName: RithmicCredentials\["systemName"\]\)[\s\S]*setRithmicAccounts\(\[\]\)[\s\S]*accountKey: undefined[\s\S]*systemName/, "Changing Rithmic environments must clear accounts and the old environment's opaque account key.");
assert.match(importPanels, /data-rithmic-environment[\s\S]*disabled=\{rithmicBusy\}[\s\S]*onChange=\{\(event\) => changeRithmicEnvironment/, "The environment cannot change underneath an in-flight account-discovery response.");
assert.match(importPanels, /data-rithmic-account[\s\S]*disabled=\{rithmicBusy\}[\s\S]*onChange=\{\(event\) => setRithmicCredentials/, "The selected account cannot change underneath an in-flight sync.");
assert.match(importPanels, /onChange=\{\(event\) => changeRithmicEnvironment\(event\.target\.value as RithmicCredentials\["systemName"\]\)\}/, "The environment selector must use the account-reset transition.");
assert.match(importPanels, /Cova makes no order or funds calls/i, "Rithmic copy must describe Cova's application behavior without inventing a provider-enforced credential scope.");
assert.doesNotMatch(importPanels, /No order access/i, "Rithmic copy must not overstate provider-enforced read-only scope.");
assert.match(importPanels, /Cova never stores it/i, "The credentials lifecycle must be stated beside the form.");
assert.match(importPanels, /gross before commissions/i, "Rithmic-imported P&L must not be presented as net P&L.");
assert.match(importPanels, /RithmicAttribution/, "The provider form must display required Rithmic and OMNE attribution.");
assert.match(importDesk, /authorizedFetch\("\/api\/rithmic\/sync"/, "Rithmic sync must use the authenticated same-origin API.");
assert.match(importDesk, /authorizedFetch\("\/api\/rithmic\/status"/, "The login form must be gated by the private connector capability check.");
assert.match(importPanels, /data-rithmic-unavailable/, "Unavailable environments must show a truthful gate instead of credential fields.");
assert.match(importPanels, /Private sync unavailable/, "Unavailable private sync must not render an enabled-status claim.");
assert.match(importPanels, /disabled=\{rithmicBusy\}/, "The Rithmic submit control must block duplicate in-flight syncs.");
assert.match(importPanels, /username: "", password: ""/, "The frontend must clear both Rithmic fields after a sync attempt.");
assert.match(importPanels, /rithmicAccounts\.map/, "Multiple Rithmic accounts must render an explicit selector.");
assert.match(importPanels, /accountKey/, "Duplicate provider account ids must be selected with an opaque composite key.");
assert.doesNotMatch(importPanels, /<option value=\{365\}>/, "The UI must not offer history ranges outside the bounded serverless runtime budget.");
assert.match(endpoint, /new Set\(\[30, 90, 180\]\)/, "The public boundary must enforce the bounded history range.");
assert.match(bridge, /AbortSignal\.timeout\(250_000\)/, "The bridge must retain cleanup and response margin inside Vercel's 300-second limit.");
assert.match(vercel, /"api\/rithmic\/status\.js"\s*:\s*\{\s*"maxDuration"\s*:\s*30\s*\}/, "The signed status check must retain margin for authentication and the private capability request.");
assert.match(importDesk, /mode: "ephemeral"/, "Rithmic history must be labeled imported, not persistently linked.");
assert.match(rithmicZeroTradeBranch, /status: "imported"[\s\S]*writeBrokerStatus\(nextStatus\)[\s\S]*return data/, "A verified zero-trade Rithmic login must persist an ephemeral sync receipt for dashboard resync.");
assert.match(rithmicZeroTradeBranch, /Already up to date/, "A successful zero-row Rithmic resync must report a truthful zero-change receipt.");
assert.match(importDesk, /const mergeReceipt = preparedImport\.commit\(data\.csv, "merge"\)/, "Rithmic resync must merge stable provider trade ids through the captured principal guard.");
assert.match(importDesk, /Already up to date/, "A zero-change resync needs a truthful receipt.");
assert.match(importDesk, /mergeReceipt\.added[\s\S]*mergeReceipt\.corrected[\s\S]*mergeReceipt\.unchanged/, "The Rithmic receipt must distinguish new, corrected, and unchanged trades.");
assert.match(app, /mergeTradeLedger\(currentTrades, acceptedTrades\)/, "The product import boundary must execute the tested account-safe merge against the latest ledger.");
assert.match(app, /const tradesRef = useRef\(trades\)[\s\S]*useEffect\(\(\) => \{\s*tradesRef\.current = trades;\s*\}, \[trades\]\)/, "All non-import ledger changes must refresh the live merge source before another user action.");
assert.match(app, /const currentTrades = tradesRef\.current/, "Imports must merge against the latest committed ledger rather than an async handler's stale render closure.");
assert.match(app, /tradesRef\.current = nextTrades;\s*setTrades\(nextTrades\)/, "The ledger ref and React state must advance atomically for the next completion.");
assert.match(app, /const authSessionRef = useRef\(authSession\)[\s\S]*useLayoutEffect\(\(\) => \{\s*authSessionRef\.current = authSession;\s*\}, \[authSession\]\)/, "Hosted and Supabase members need a live in-memory principal source updated before interaction rather than the intentionally non-durable auth loader.");
assert.match(importPrincipalBlock, /const session = authSessionRef\.current/, "Rithmic import principal capture must use the active hosted session.");
assert.doesNotMatch(importPrincipalBlock, /loadAuthSession\(/, "The Rithmic import principal boundary must not reject valid hosted sessions through the persistence loader.");
assert.match(app, /function prepareImportCsv\(\)[\s\S]*const principalAtStart = getCurrentImportPrincipal\(\)[\s\S]*const isCurrent = \(\) => isImportPrincipalCurrent\(principalAtStart, getCurrentImportPrincipal\(\)\)[\s\S]*return \{[\s\S]*isCurrent,[\s\S]*commit: \(text: string, mode: ImportMode = "append"\)[\s\S]*return importCsv\(text, mode\)/, "A completed async import must revalidate the initiating Cova principal before every state or receipt write.");
assert.match(importDesk, /const rithmicRequestRef = useRef<AbortController \| null>\(null\)/, "Rithmic requests must have a component-owned abort handle.");
assert.match(importDesk, /rithmicRequestGenerationRef\.current \+= 1;[\s\S]*rithmicRequestRef\.current\?\.abort\(\)/, "Unmounting the import route must invalidate and abort the active Rithmic completion.");
assert.match(importDesk, /const preparedImport = prepareImportCsv\(\)[\s\S]*const controller = new AbortController\(\)[\s\S]*signal: controller\.signal/, "Rithmic sync must capture the principal before network I\/O and bind the request to its lifecycle signal.");
assert.match(importDesk, /controller\.signal\.aborted \|\| requestGeneration !== rithmicRequestGenerationRef\.current \|\| !preparedImport\.isCurrent\(\)/, "Every completion path, including a zero-trade receipt, must reject an aborted, superseded, or cross-principal request before state writes.");
assert.match(rithmicFinallyBlock, /setRithmicBusy\(false\)/, "Rithmic busy state must always settle for the active request generation.");
assert.doesNotMatch(rithmicFinallyBlock, /preparedImport\.isCurrent\(\)/, "A benign principal invalidation must not leave Sync history permanently disabled.");
assert.match(app, /mode === "replace" && brokerStatus\?\.mode === "ephemeral"[\s\S]*clearBrokerStatus\(\)/, "Only a replacement CSV import may clear the last Rithmic import status.");
assert.match(app, /reset=\{\(\) => \{[\s\S]*clearBrokerStatus\(\)[\s\S]*cova:broker-status/, "Resetting the demo must clear ephemeral Rithmic status too.");
assert.match(dashboard, /trade\.source\?\.provider === "Rithmic"/, "Rithmic attribution must follow the imported trades, including mixed datasets.");
assert.match(dashboard, /Sync new trades/, "A Rithmic ledger must expose an explicit dashboard resync action.");
assert.match(app, /rithmicSyncAvailable=\{brokerStatus\?\.provider === "Rithmic" && brokerStatus\.status === "imported"\}/, "A successful zero-trade Rithmic sync must keep the dashboard resync action available.");
assert.match(dashboard, /const hasRithmicTrades = analysis\.trades\.some/, "Rithmic resync discovery must use the complete ledger, not the currently filtered dashboard range.");
assert.match(dashboard, /hasRithmicTrades \|\| rithmicSyncAvailable/, "Rithmic resync discovery must survive a valid zero-trade import receipt.");
assert.match(dashboard, /const sourceLabel = getTradeSourceLabel\(scopedAnalysis\.trades\)/, "A zero-trade Rithmic receipt must not relabel unrelated ledger rows as Rithmic data.");
assert.match(dashboard, /sessionStorage\.setItem\(IMPORT_PROVIDER_HINT_KEY, "rithmic"\)/, "The resync action must preserve the provider handoff.");
assert.match(dashboard, /dispatchEvent\(new CustomEvent\("cova:import-provider", \{ detail: "rithmic" \}\)\)/, "The provider handoff must survive route-exit animation overlap.");
assert.match(importDesk, /detail !== "rithmic"[\s\S]*setSelectedFirmId\("rithmic"\)/, "A still-mounted import route must accept the provider handoff immediately.");
assert.match(importDesk, /addEventListener\("cova:import-provider", selectRequestedProvider\)/, "The provider handoff listener must remain active during route transitions.");
assert.doesNotMatch(importDesk, /const selectRequestedProvider[\s\S]*?sessionStorage\.removeItem\(IMPORT_PROVIDER_HINT_KEY\)[\s\S]*?setSelectedFirmId\("rithmic"\)/, "An exiting import route must not consume the hint before the entering route mounts.");
assert.match(importDesk, /setSelectedFirmId\("rithmic"\)[\s\S]*?window\.setTimeout\([\s\S]*?sessionStorage\.removeItem\(IMPORT_PROVIDER_HINT_KEY\)/, "A provider hint handled in place must be cleared only after the route transition window.");
assert.match(importDesk, /function readImportProviderHint\(\)[\s\S]*sessionStorage\.getItem\(IMPORT_PROVIDER_HINT_KEY\)[\s\S]*if \(hint === "rithmic"\) return hint/, "StrictMode initializers must read the provider hint without consuming it.");
assert.doesNotMatch(importDesk, /function readImportProviderHint\(\)[\s\S]*sessionStorage\.removeItem\(IMPORT_PROVIDER_HINT_KEY\)[\s\S]*return "topstepx"/, "A StrictMode double initializer must not consume the provider hint on its discarded pass.");
assert.match(importDesk, /useState<PropFirmId>\(readImportProviderHint\)/, "The import route must initialize from the preserved provider hint.");
assert.match(importDesk, /useEffect\(\(\) => \{[\s\S]*sessionStorage\.removeItem\(IMPORT_PROVIDER_HINT_KEY\)[\s\S]*\}, \[\]\)/, "The provider hint must be cleared only after mount without triggering a sync.");
assert.match(attribution, /Trading Platform by Rithmic™ is a trademark of Rithmic, LLC/, "The required Rithmic trademark notice must be present.");
assert.match(attribution, /The R \| Protocol API™ software is Copyright © 2026 by Rithmic, LLC/, "The required protocol copyright notice must be present.");
assert.match(attribution, /The OMNE™ software is Copyright © 2026 by Omnesys, LLC and Omnesys Technologies, Inc\./, "The required OMNE copyright notice must be present.");
assert.match(attribution, /The Powered by OMNE artwork is a trademark of Omnesys, LLC and Omnesys Technologies, Inc\. All Rights Reserved\./, "The required OMNE artwork notice must be present verbatim.");
assert.match(endpoint, /requirePolicyAcceptedUser/);
assert.match(endpoint, /requireProEntitlement/);
assert.match(endpoint, /acquireRithmicSyncPermit/, "The public sync route must rate-limit before calling the private connector.");
assert.match(limiter, /\["SET", lockKey, lockId, "NX", "EX", LOCK_SECONDS\]/, "Concurrent syncs must be blocked atomically.");
assert.match(statusEndpoint, /requirePolicyAcceptedUser/);
assert.match(statusEndpoint, /requireProEntitlement/);
assert.doesNotMatch(endpoint, /console\.(log|info|debug)/, "The credential-handling endpoint must not log request data.");
assert.doesNotMatch(endpoint, /broker_connections|serviceSupabase|access_token_encrypted|refresh_token_encrypted/, "Rithmic credentials must not be persisted by the public endpoint.");

const rithmicLogo = ["public", "media", "rithmic", "trading-platform-by-rithmic.png"];
const omneLogo = ["public", "media", "rithmic", "powered-by-omne.png"];
assert.ok(existsSync(path.join(root, ...rithmicLogo)) && existsSync(path.join(root, ...omneLogo)), "Official provider artwork must be checked in.");
assert.equal(hash(...rithmicLogo), "187febae74c28fc370fe27a8196ba81ccc89af1150b1f641ad6b25ea5fa48cd8", "Rithmic artwork must remain byte-identical to the provider file.");
assert.equal(hash(...omneLogo), "7af9acab16c4d75ddc11efd0e1011230a8cb33ffe1ea01e362706f6c70957994", "OMNE artwork must remain byte-identical to the provider file.");

console.log("rithmic ui/security regression: passed");
